'use client';
import { useMemo, useState } from 'react';
import Link from 'next/link';
import { ArrowRight, ChevronDown, Minus, Plus, ShoppingBag, UtensilsCrossed } from 'lucide-react';
import type { MenuItem } from '@/lib/menu';
const C=[['Breakfast','Ethiopian and continental breakfast favorites.'],['Ethiopian Favorites','Tule classics with the flavors of Ethiopia.'],['Main Courses','Grilled, hearty and resort-style dishes.'],['Pizza & Pasta','Easygoing Italian-inspired favorites.'],['Burgers & Sandwiches','Casual choices for lunch and relaxed dining.'],['Desserts & Drinks','Sweet finishes, coffee and fresh refreshments.']] as const;
const A:Record<string,string>={Ethiopian:'Ethiopian Favorites',Mains:'Main Courses',Pizza:'Pizza & Pasta',Pasta:'Pizza & Pasta',Desserts:'Desserts & Drinks',Drinks:'Desserts & Drinks','Fish & Seafood':'Main Courses',Sides:'Main Courses'};
export default function RestaurantMenuClient({items}:{items:MenuItem[]}){const[cat,setCat]=useState<string|null>(null);const[all,setAll]=useState(false);const[cart,setCart]=useState<Record<string,number>>({});const grouped=useMemo(()=>{const m=new Map<string,MenuItem[]>();for(const i of items){const k=A[i.category]??i.category;const v=m.get(k)??[];v.push(i);m.set(k,v)}return m},[items]);const visible=cat?(grouped.get(cat)??[]):all?items:items.slice(0,6);const total=Object.values(cart).reduce((a,b)=>a+b,0);const change=(id:string,d:number)=>setCart(x=>{const n={...x},q=Math.max(0,(n[id]??0)+d);if(q)n[id]=q;else delete n[id];if(typeof window!=='undefined')window.localStorage.setItem('tule-resort-cart',JSON.stringify(n));return n});return <main id="menu" className="mx-auto max-w-7xl px-4 py-12 md:px-6 md:py-16"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.25em] text-[#0B4F6C]/55">Restaurant menu</p><h2 className="mt-2 text-4xl font-black md:text-5xl">Simple. Clear. Delicious.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-[#073B4C]/55">Six simple sections keep dining easy to browse. The complete menu stays one click away.</p></div>{total>0&&<Link href="/guest/order" className="inline-flex items-center gap-2 rounded-full bg-[#073B4C] px-5 py-3 text-xs font-black uppercase tracking-[.13em] text-white"><ShoppingBag className="h-4 w-4"/>Review order ({total})</Link>}</div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{C.map(([label,desc])=>{const count=grouped.get(label)?.length??0;return <button key={label} type="button" onClick={()=>setCat(cat===label?null:label)} className={`rounded-3xl border p-5 text-left ${cat===label?'border-[#C9A227] bg-[#F3E9D2]':'border-[#0B4F6C]/10 bg-white hover:shadow-lg'}`}><div className="flex items-start justify-between gap-4"><div><span className="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-[#EAF4F7] text-[#0B4F6C]"><UtensilsCrossed className="h-5 w-5"/></span><h3 className="mt-4 font-black">{label}</h3><p className="mt-2 text-sm leading-6 text-[#073B4C]/55">{desc}</p></div><span className="rounded-full bg-[#F8FBFC] px-3 py-1 text-[10px] font-black">{count}</span></div></button>})}</div><div className="mt-14 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.25em] text-[#C9A227]">{cat??(all?'Full menu':'Featured')}</p><h3 className="mt-2 text-3xl font-black">{cat??(all?'All menu items':'Guest favorites')}</h3></div><div className="flex gap-2">{cat&&<button type="button" onClick={()=>setCat(null)} className="rounded-full border border-[#0B4F6C]/10 bg-white px-4 py-2 text-[10px] font-black uppercase tracking-[.12em]">Clear category</button>}{!cat&&<button type="button" onClick={()=>setAll(!all)} className="inline-flex items-center gap-2 rounded-full bg-[#C9A227] px-5 py-3 text-[10px] font-black uppercase tracking-[.12em]">{all?'Show featured':'View full menu'}<ChevronDown className={`h-4 w-4 ${all?'rotate-180':''}`}/></button>}</div></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{visible.map(i=>{const q=cart[i.id]??0;return <article key={i.id} className="overflow-hidden rounded-3xl border border-[#0B4F6C]/10 bg-white shadow-[0_15px_45px_rgba(7,59,76,.06)]"><div className="h-44 bg-[#EAF4F7]">{i.imageUrl?<img src={i.imageUrl} alt={i.name} className="h-full w-full object-cover" loading="lazy"/>:<div className="flex h-full items-center justify-center text-[#0B4F6C]/30"><UtensilsCrossed className="h-10 w-10"/></div>}</div><div className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-[9px] font-black uppercase tracking-[.16em] text-[#C9A227]">{i.amharicName}</p><h4 className="mt-1 font-black">{i.name}</h4></div><span className="shrink-0 text-sm font-black text-[#0B4F6C]">{i.price.toLocaleString()} ETB</span></div><p className="mt-3 text-sm leading-6 text-[#073B4C]/55">{i.description}</p><div className="mt-5 flex items-center justify-between gap-3">{q===0?<button type="button" onClick={()=>change(i.id,1)} className="inline-flex items-center gap-2 rounded-full bg-[#073B4C] px-4 py-3 text-[10px] font-black uppercase tracking-[.12em] text-white">Add to order <ArrowRight className="h-3.5 w-3.5"/></button>:<div className="inline-flex items-center gap-2 rounded-full border border-[#0B4F6C]/10 bg-[#F8FBFC] p-1"><button type="button" onClick={()=>change(i.id,-1)} className="flex h-8 w-8 items-center justify-center rounded-full bg-white"><Minus className="h-3.5 w-3.5"/></button><span className="min-w-6 text-center text-xs font-black">{q}</span><button type="button" onClick={()=>change(i.id,1)} className="flex h-8 w-8 items-center justify-center rounded-full bg-[#073B4C] text-white"><Plus className="h-3.5 w-3.5"/></button></div>}{q>0&&<Link href="/guest/order" className="text-[10px] font-black uppercase tracking-[.12em] text-[#0B4F6C]">View bag →</Link>}</div></div></article>})}</div></main>